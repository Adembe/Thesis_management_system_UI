
export interface  User {
	id          ?: number;
	name        ?: string;   
	email       ?: string;   
	password    ?: string;   
	type        ?: number;     
	phone_number ?: string;  
	address     ?: string;  
	createdAt   ?: Date;
	updatedAt   ?: Date;
}